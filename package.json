{
  "name": "giveawaytools",
  "version": "1.0.0",
  "description": "A tool to choose the winner that respect the giveaway rules",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:build": "rm -rf ./dist && tsc && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ora": "^4.0.3",
    "progress": "^2.0.3",
    "puppeteer": "^10.2.0"
  },
  "devDependencies": {
    "@types/progress": "^2.0.5",
    "eslint": "^7.32.0",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.4.3"
  }
}
